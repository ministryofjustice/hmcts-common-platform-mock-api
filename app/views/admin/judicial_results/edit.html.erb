<%= form_with(model: @judicial_result, local: true, url: admin_judicial_result_path) do |form| %>
  <%= render 'form', f: form %>
  <%= form.submit %>
<% end %>

<hr>

<%= link_to 'Back', admin_hearing_path(@judicial_result.hearing_id) %>
