<%= form_with(model: @court_application, local: true, url: admin_court_application_path) do |form| %>
  <%= render 'form', f: form %>
  <%= form.submit %>
<% end %>

<hr>

<%= link_to 'Back', admin_hearing_path(@court_application.hearing_id) %>