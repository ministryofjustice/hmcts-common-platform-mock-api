<p id="notice"><%= notice %></p>

<h1>Hearing Day</h1>

<% @hearing_day.attributes.each do |key, value| %>
  <p><strong><%= key %></strong>: <%= value %></p>
<% end %>

<hr />

<p><%= link_to "Edit hearing day", edit_hearing_day_admin_hearing_path(@hearing, @hearing_day) %></p>
<p><%= link_to "Delete hearing day", delete_hearing_day_admin_hearing_path(@hearing, @hearing_day), method: "DELETE" %></p>
