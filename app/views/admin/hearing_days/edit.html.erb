<%= form_with(model: @hearing_day, local: true, url: admin_hearing_day_path) do |form| %>
  <%= render 'form', f: form %>
  <%= form.submit %>
<% end %>

<hr>

<%= link_to 'Back', admin_hearing_path(@hearing_day.hearing) %>
